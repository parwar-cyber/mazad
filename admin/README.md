# Mazad — Admin Console (Phase 9 placeholder)

The Next.js admin console lives here.  Scaffolding lands in **Phase 9** per
`docs/architecture.md` §11.  This file exists at Phase 0 so the repo layout
matches the spec and reviewers can see the intended home.

Until Phase 9 begins, ops procedures run via direct SQL — see
`docs/runbook.md` (force-update kill switch, feature flag toggles).
